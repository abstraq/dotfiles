-- mason.nvim (Package Manager)
-- https://github.com/williamboman/mason.nvim
return {
	"williamboman/mason.nvim",
	opts = {
		ui = {
			icons = {
				package_installed = "",
				package_uninstalled = "",
				package_pending = ""
			}
		}
	}
}
